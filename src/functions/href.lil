import { undef, isUndef, isNullish, isFn, isStr, isNum, isArr, toStr, toNum, toInt, len, hasOwn, inOp, deleteKey, instanceOf, push, newArray, emptyObject, warn, getDocument, getConsole, globalDefined, globalGet, mathMin, mathMax, constructN, apply, objectAssign, objectKeys, makeError, makeTypeError, throwErr, hostParseInt, hostParseFloat, fromCharCode, Error, TypeError, Object, Array, Function, String, Number, Math, RegExp, JSON, Infinity, NaN } from "../host.lil";

import { defineFunction, ordargument } from "../defineFunction.lil";
import { buildCommon } from "../buildCommon.lil";
import { assertNodeType } from "../parseNode.lil";
import { MathNode } from "../mathMLTree.lil";
import { __star as html } from "../buildHTML.lil";
import { __star as mml } from "../buildMathML.lil";

JsValue o1 = JS.object("type", "href", "names", JS.array("\\href"));
JsValue o3 = JS.object("numArgs", 2, "argTypes", JS.array("url", "original"), "allowedInText", true);
o1["props"] = o3;
o1["handler"] = (JsValue p5, JsValue args) => {
  JsValue op6 = p5;
  JsValue parser = op6["parser"];
  JsValue body = args[1];
  JsValue href = JS.call(assertNodeType, undef(), args[0], "url")["url"];
  JsValue o7 = JS.object("command", "\\href", "url", href);
  if (!(JS.invoke(parser["settings"], "isTrusted", o7).truthy())) {
    return JS.invoke(parser, "formatUnsupportedCmd", "\\href");
  }
  JsValue o8 = JS.object("type", "href", "mode", parser["mode"], "href", href, "body", JS.call(ordargument, undef(), body));
  return o8;
  return undef();
};
o1["htmlBuilder"] = (JsValue group, JsValue options) => {
  JsValue elements = JS.invoke(html, "buildExpression", group["body"], options, false);
  JsValue a9 = JS.array();
  return JS.invoke(buildCommon, "makeAnchor", group["href"], a9, elements, options);
  return undef();
};
o1["mathmlBuilder"] = (JsValue group, JsValue options) => {
  JsValue math = JS.invoke(mml, "buildExpressionRow", group["body"], options);
  if (!(instanceOf(math, MathNode))) {
    JsValue a10 = JS.array(math);
    math = JS.construct(MathNode, "mrow", a10);
  }
  JS.invoke(math, "setAttribute", "href", group["href"]);
  return math;
  return undef();
};
JS.call(defineFunction, undef(), o1);
JsValue o11 = JS.object("type", "href", "names", JS.array("\\url"));
JsValue o13 = JS.object("numArgs", 1, "argTypes", JS.array("url"), "allowedInText", true);
o11["props"] = o13;
o11["handler"] = (JsValue p15, JsValue args) => {
  JsValue op16 = p15;
  JsValue parser = op16["parser"];
  JsValue href = JS.call(assertNodeType, undef(), args[0], "url")["url"];
  JsValue o17 = JS.object("command", "\\url", "url", href);
  if (!(JS.invoke(parser["settings"], "isTrusted", o17).truthy())) {
    return JS.invoke(parser, "formatUnsupportedCmd", "\\url");
  }
  JsValue a18 = JS.array();
  JsValue chars = a18;
  {
    JsValue i = 0;
    while (JS.lessThan(i, href["length"])) {
      JsValue c = href[i];
      if (JS.strictEqual(c, "~")) {
        c = "\\textasciitilde";
      }
      JsValue o19 = JS.object("type", "textord", "mode", "text", "text", c);
      JS.invoke(chars, "push", o19);
      JsValue u20 = i;
      i = JS.add(i, 1);
    }
  }
  JsValue o21 = JS.object("type", "text", "mode", parser["mode"], "font", "\\texttt", "body", chars);
  JsValue body = o21;
  JsValue o22 = JS.object("type", "href", "mode", parser["mode"], "href", href, "body", JS.call(ordargument, undef(), body));
  return o22;
  return undef();
};
JS.call(defineFunction, undef(), o11);
JsValue __star = emptyObject();

export { __star };
