import { undef, isUndef, isNullish, isFn, isStr, isNum, isArr, toStr, toNum, toInt, len, hasOwn, inOp, deleteKey, instanceOf, push, newArray, emptyObject, warn, getDocument, getConsole, globalDefined, globalGet, mathMin, mathMax, constructN, apply, objectAssign, objectKeys, makeError, makeTypeError, throwErr, hostParseInt, hostParseFloat, fromCharCode, Error, TypeError, Object, Array, Function, String, Number, Math, RegExp, JSON, Infinity, NaN } from "../host.lil";

import { binrelClass } from "./mclass.lil";
import { defineFunction, normalizeArgument } from "../defineFunction.lil";
import { utils } from "../utils.lil";
import { __star as html } from "../buildHTML.lil";
import { __star as mml } from "../buildMathML.lil";

JsValue htmlBuilder = undef();
JsValue mathmlBuilder = undef();
JsValue fontAliases = undef();

htmlBuilder = (JsValue group, JsValue options) => {
  JsValue font = group["font"];
  JsValue newOptions = JS.invoke(options, "withFont", font);
  return JS.invoke(html, "buildGroup", group["body"], newOptions);
  return undef();
};
mathmlBuilder = (JsValue group, JsValue options) => {
  JsValue font = group["font"];
  JsValue newOptions = JS.invoke(options, "withFont", font);
  return JS.invoke(mml, "buildGroup", group["body"], newOptions);
  return undef();
};
JsValue o1 = JS.object("\\Bbb", "\\mathbb", "\\bold", "\\mathbf", "\\frak", "\\mathfrak", "\\bm", "\\boldsymbol");
fontAliases = o1;
JsValue o2 = JS.object("type", "font", "names", JS.array("\\mathrm", "\\mathit", "\\mathbf", "\\mathnormal", "\\mathsfit", "\\mathbb", "\\mathcal", "\\mathfrak", "\\mathscr", "\\mathsf", "\\mathtt", "\\Bbb", "\\bold", "\\frak"));
JsValue o4 = JS.object("numArgs", 1, "allowedInArgument", true);
o2["props"] = o4;
o2["handler"] = (JsValue p5, JsValue args) => {
  JsValue op6 = p5;
  JsValue parser = op6["parser"];
  JsValue funcName = op6["funcName"];
  JsValue body = JS.call(normalizeArgument, undef(), args[0]);
  JsValue funcVal = funcName;
  if (inOp(funcVal, fontAliases)) {
    funcVal = fontAliases[funcVal];
  }
  JsValue o7 = JS.object("type", "font", "mode", parser["mode"], "font", JS.invoke(funcVal, "slice", 1), "body", body);
  return o7;
  return undef();
};
o2["htmlBuilder"] = htmlBuilder;
o2["mathmlBuilder"] = mathmlBuilder;
JS.call(defineFunction, undef(), o2);
JsValue o8 = JS.object("type", "mclass", "names", JS.array("\\boldsymbol", "\\bm"));
JsValue o10 = JS.object("numArgs", 1);
o8["props"] = o10;
o8["handler"] = (JsValue p11, JsValue args) => {
  JsValue op12 = p11;
  JsValue parser = op12["parser"];
  JsValue body = args[0];
  JsValue isCharacterBox = JS.invoke(utils, "isCharacterBox", body);
  JsValue o13 = JS.object("type", "mclass", "mode", parser["mode"], "mclass", JS.call(binrelClass, undef(), body));
  JsValue a14 = JS.array(JS.object("type", "font", "mode", parser["mode"], "font", "boldsymbol", "body", body));
  o13["body"] = a14;
  o13["isCharacterBox"] = isCharacterBox;
  return o13;
  return undef();
};
JS.call(defineFunction, undef(), o8);
JsValue o16 = JS.object("type", "font", "names", JS.array("\\rm", "\\sf", "\\tt", "\\bf", "\\it", "\\cal"));
JsValue o18 = JS.object("numArgs", 0, "allowedInText", true);
o16["props"] = o18;
o16["handler"] = (JsValue p19, JsValue args) => {
  JsValue op20 = p19;
  JsValue parser = op20["parser"];
  JsValue funcName = op20["funcName"];
  JsValue breakOnTokenText = op20["breakOnTokenText"];
  JsValue op21 = parser;
  JsValue mode = op21["mode"];
  JsValue body = JS.invoke(parser, "parseExpression", true, breakOnTokenText);
  JsValue style = "math" + toStr(JS.invoke(funcName, "slice", 1));
  JsValue o22 = JS.object("type", "font", "mode", mode, "font", style);
  JsValue o23 = JS.object("type", "ordgroup", "mode", parser["mode"], "body", body);
  o22["body"] = o23;
  return o22;
  return undef();
};
o16["htmlBuilder"] = htmlBuilder;
o16["mathmlBuilder"] = mathmlBuilder;
JS.call(defineFunction, undef(), o16);
JsValue __star = emptyObject();

export { __star };
