import { undef, isUndef, isNullish, isFn, isStr, isNum, isArr, toStr, toNum, toInt, len, hasOwn, inOp, deleteKey, instanceOf, push, newArray, emptyObject, warn, getDocument, getConsole, globalDefined, globalGet, mathMin, mathMax, constructN, apply, objectAssign, objectKeys, makeError, makeTypeError, throwErr, hostParseInt, hostParseFloat, fromCharCode, Error, TypeError, Object, Array, Function, String, Number, Math, RegExp, JSON, Infinity, NaN, pushAll } from "./host.lil";

JsValue scriptData = undef();
JsValue scriptFromCodepoint = undef();
JsValue allBlocks = undef();
JsValue supportedCodepoint = undef();

JsValue a1 = JS.array(JS.object("name", "latin", "blocks", JS.array(JS.array(256, 591), JS.array(768, 879))), JS.object("name", "cyrillic", "blocks", JS.array(JS.array(1024, 1279))), JS.object("name", "armenian", "blocks", JS.array(JS.array(1328, 1423))), JS.object("name", "brahmic", "blocks", JS.array(JS.array(2304, 4255))), JS.object("name", "georgian", "blocks", JS.array(JS.array(4256, 4351))), JS.object("name", "cjk", "blocks", JS.array(JS.array(12288, 12543), JS.array(19968, 40879), JS.array(65280, 65376))), JS.object("name", "hangul", "blocks", JS.array(JS.array(44032, 55215))));
scriptData = a1;
scriptFromCodepoint = (JsValue codepoint) => {
  {
    JsValue i = 0;
    while (JS.lessThan(i, scriptData["length"])) {
      JsValue script = scriptData[i];
      {
        JsValue i = 0;
        while (JS.lessThan(i, script["blocks"]["length"])) {
          JsValue block = script["blocks"][i];
          if ((JS.greaterThanOrEqual(codepoint, block[0])) && (JS.lessThanOrEqual(codepoint, block[1]))) {
            return script["name"];
          }
          JsValue u26 = i;
          i = JS.add(i, 1);
        }
      }
      JsValue u27 = i;
      i = JS.add(i, 1);
    }
  }
  return null;
  return undef();
};
JsValue a28 = newArray();
allBlocks = a28;
JS.invoke(scriptData, "forEach", (JsValue s) => {
  return JS.invoke(s["blocks"], "forEach", (JsValue b) => {
  pushAll(allBlocks, b);
  return undef();
  });
});
supportedCodepoint = (JsValue codepoint) => {
  {
    JsValue i = 0;
    while (JS.lessThan(i, allBlocks["length"])) {
      if ((JS.greaterThanOrEqual(codepoint, allBlocks[i])) && (JS.lessThanOrEqual(codepoint, allBlocks[JS.add(i, 1)]))) {
        return true;
      }
      i = JS.add(i, 2);
    }
  }
  return false;
  return undef();
};
JsValue __star = JS.object("scriptFromCodepoint", scriptFromCodepoint, "supportedCodepoint", supportedCodepoint);

export { __star, scriptFromCodepoint, supportedCodepoint };
