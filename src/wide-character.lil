import { undef, isUndef, isNullish, isFn, isStr, isNum, isArr, toStr, toNum, toInt, len, hasOwn, inOp, deleteKey, instanceOf, push, newArray, emptyObject, warn, getDocument, getConsole, globalDefined, globalGet, mathMin, mathMax, constructN, apply, objectAssign, objectKeys, makeError, makeTypeError, throwErr, hostParseInt, hostParseFloat, fromCharCode, Error, TypeError, Object, Array, Function, String, Number, Math, RegExp, JSON, Infinity, NaN } from "./host.lil";

import { ParseError } from "./ParseError.lil";

JsValue wideLatinLetterData = undef();
JsValue wideNumeralData = undef();
JsValue wideCharacterFont = undef();

JsValue a1 = JS.array(JS.array("mathbf", "textbf", "Main-Bold"), JS.array("mathbf", "textbf", "Main-Bold"), JS.array("mathnormal", "textit", "Math-Italic"), JS.array("mathnormal", "textit", "Math-Italic"), JS.array("boldsymbol", "boldsymbol", "Main-BoldItalic"), JS.array("boldsymbol", "boldsymbol", "Main-BoldItalic"), JS.array("mathscr", "textscr", "Script-Regular"), JS.array("", "", ""), JS.array("", "", ""), JS.array("", "", ""), JS.array("mathfrak", "textfrak", "Fraktur-Regular"), JS.array("mathfrak", "textfrak", "Fraktur-Regular"), JS.array("mathbb", "textbb", "AMS-Regular"), JS.array("mathbb", "textbb", "AMS-Regular"), JS.array("mathboldfrak", "textboldfrak", "Fraktur-Regular"), JS.array("mathboldfrak", "textboldfrak", "Fraktur-Regular"), JS.array("mathsf", "textsf", "SansSerif-Regular"), JS.array("mathsf", "textsf", "SansSerif-Regular"), JS.array("mathboldsf", "textboldsf", "SansSerif-Bold"), JS.array("mathboldsf", "textboldsf", "SansSerif-Bold"), JS.array("mathitsf", "textitsf", "SansSerif-Italic"), JS.array("mathitsf", "textitsf", "SansSerif-Italic"), JS.array("", "", ""), JS.array("", "", ""), JS.array("mathtt", "texttt", "Typewriter-Regular"), JS.array("mathtt", "texttt", "Typewriter-Regular"));
wideLatinLetterData = a1;
JsValue a28 = JS.array(JS.array("mathbf", "textbf", "Main-Bold"), JS.array("", "", ""), JS.array("mathsf", "textsf", "SansSerif-Regular"), JS.array("mathboldsf", "textboldsf", "SansSerif-Bold"), JS.array("mathtt", "texttt", "Typewriter-Regular"));
wideNumeralData = a28;
wideCharacterFont = (JsValue wideChar, JsValue mode) => {
  JsValue H = JS.invoke(wideChar, "charCodeAt", 0);
  JsValue L = JS.invoke(wideChar, "charCodeAt", 1);
  JsValue codePoint = JS.add(JS.add((toNum((toNum(H) - toNum(55296))) * toNum(1024)), (toNum(L) - toNum(56320))), 65536);
  JsValue c34 = undef();
  if (JS.strictEqual(mode, "math")) {
    c34 = 0;
  } else {
    c34 = 1;
  }
  JsValue j = c34;
  if ((JS.lessThanOrEqual(119808, codePoint)) && (JS.lessThan(codePoint, 120484))) {
    JsValue i = JS.invoke(Math, "floor", (toNum((toNum(codePoint) - toNum(119808))) / toNum(26)));
    JsValue a35 = JS.array(wideLatinLetterData[i][2], wideLatinLetterData[i][j]);
    return a35;
  } else {
    if ((JS.lessThanOrEqual(120782, codePoint)) && (JS.lessThanOrEqual(codePoint, 120831))) {
      JsValue i = JS.invoke(Math, "floor", (toNum((toNum(codePoint) - toNum(120782))) / toNum(10)));
      JsValue a36 = JS.array(wideNumeralData[i][2], wideNumeralData[i][j]);
      return a36;
    } else {
      if ((JS.strictEqual(codePoint, 120485)) || (JS.strictEqual(codePoint, 120486))) {
        JsValue a37 = JS.array(wideLatinLetterData[0][2], wideLatinLetterData[0][j]);
        return a37;
      } else {
        if ((JS.lessThan(120486, codePoint)) && (JS.lessThan(codePoint, 120782))) {
          JsValue a38 = JS.array("", "");
          return a38;
        } else {
          throw JS.construct(ParseError, JS.add("Unsupported character: ", wideChar));
        }
      }
    }
  }
  return undef();
};
JsValue __star = JS.object("wideCharacterFont", wideCharacterFont);

export { __star, wideCharacterFont };
